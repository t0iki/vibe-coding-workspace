name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'docs/ruleclicker/**'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: docs/ruleclicker/package-lock.json

      - name: Install dependencies
        working-directory: docs/ruleclicker
        run: npm ci

      - name: Build
        working-directory: docs/ruleclicker
        run: npm run build

      - name: Prepare deployment directory
        run: |
          mkdir -p ./deploy
          cp -r docs/ruleclicker/dist/* ./deploy/
          mkdir -p ./deploy/ruleclicker
          cp -r docs/ruleclicker/dist/* ./deploy/ruleclicker/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./deploy

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4